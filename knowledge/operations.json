{
  "operations": {
    "ASSERT_STATE": {
      "target": "MEMORY",
      "kind": "FACT",
      "execution": "STORE_RELATION",
      "requires": ["subject", "predicate", "object"],
      "responses": {
        "success": "I have stored that in memory.",
        "conflict": "Memory created, but it conflicts with an existing memory."
      }
    },
    "ASSERT_RELATION": {
      "target": "MEMORY",
      "kind": "RELATION",
      "execution": "STORE_RELATION",
      "requires": ["subject", "predicate", "object"],
      "responses": {
        "success": "I have stored that in memory.",
        "conflict": "Memory created, but it conflicts with an existing memory."
      }
    },
    "ASSERT_CLASSIFICATION": {
      "target": "MEMORY",
      "kind": "CLASSIFICATION",
      "execution": "STORE_RELATION",
      "requires": ["subject", "predicate", "object"],
      "responses": {
        "success": "Understood. I know that {subject_name} is a {object_word}.",
        "failure": "I don't know that type yet."
      }
    }
  }
}
